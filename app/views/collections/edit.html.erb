<h1>Edit Collection</h1>
<%= form_with(model: [@user, @collection]) do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name, value: @collection.name %>
  </div>

  <%= form.submit %>
<% end %>
