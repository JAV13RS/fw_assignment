<nav class="navbar navbar-expand-lg navbar-light bg-secondary sticky-top">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="navbar-text d-flex align-items-center text-white">Welcome, <%= current_user.email %></span>
          </li>
          <li class="nav-item">
            <%= link_to "My Collections", collections_path, class: "btn btn-secondary" %>
          </li>
          <li class="nav-item">
            <%= link_to "Flashcard Sets", flashcard_sets_path, class: "btn btn-secondary" %>
          </li>
          <li class="nav-item">
            <%= link_to "All Collections", all_collections_path, class: "btn btn-secondary" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Favourites', favorites_path, class: 'btn btn-secondary' %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav ms-auto">

        <% if current_user.admin? %>
          <li class="nav-item">
            <%= link_to "Settings", edit_admin_settings_path, class: "btn btn-secondary" %>
          </li>
        <% end %>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-secondary", data: { turbo_method: :delete } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: "btn btn-secondary" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-secondary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
